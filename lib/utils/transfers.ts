import type { ExtractedTransaction } from '@/types'

// Stub — real transfer detection wired in step 5
export function detectTransfers(transactions: ExtractedTransaction[]): ExtractedTransaction[] {
  return transactions
}
